<%= button_to({ action: 'new' }, class: 'button is-rounded is-primary') do %>
  <%= svg_tag 'plus' %>
  <span><%= t('.new.title') %></span>
<% end %>
<%= button_to({ action: 'invite_provider' }, class: 'button is-rounded is-primary') do %>
  <%= svg_tag 'qrcode' %>
  <span><%= t('.invite_provider.title') %></span>
<% end %>
