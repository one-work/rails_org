<%= form_with theme: 'default_phone', model: @member, url: { action: 'create' }, data: { controller: 'member' } do |f| %>
  <%= render partial: 'form', locals: { f: f } %>
  <%= f.submit '提交', wrap: { all: nil } %>
<% end %>
